//NOTE - doesnt work need fix
// export const handleSelectProducts = ({ id, price, products, fn }: { id: number; price: number; products: any[]; fn: ([]) => void }) => {
//   const isSelected = products.some((product: { id: number }) => product.id === id)
//   fn(isSelected ? products.filter((product: { id: number }) => product.id !== id) : [...products, { id, price }])
// }
